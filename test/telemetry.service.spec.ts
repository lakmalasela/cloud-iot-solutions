import { TelemetryService } from '../src/telemetry/telemetry.service';

describe('TelemetryService (unit stub tests)', () => {
  it('should be defined', () => {
    expect(TelemetryService).toBeDefined();
  });
});
